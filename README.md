# GLib Mock (Mocking framework for unit testing)

GLib Mock is a (currently) prototype framework for mocking non-GObject C functions, aiming to be added to the GLib Testing framework as the standardized way to create mocks.
